<view class="slot-machine-container">
	<view class="reel-container">
		<view wx:for="{{reels}}" wx:key="index" class="reel reel{{index}}" style="top:{{item.top}}rpx" > </view>
		<view class="reel-overlay" />
	</view>
</view>

<button class="{{spinDisabled?'disabled':''}}" bindtap="spin">开始</button>