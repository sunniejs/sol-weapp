<view>
  <view class="wheel_wp sol-class">
    <block wx:if="{{mode == 1}}">
      <image src="./images/s-dial_bg.png" />
      <view class="wheel_pointer" bindtap="start" style="-webkit-transform:rotate({{deg}}deg) translateZ(0);transform:rotate({{deg}}deg) translateZ(0)">
        <image src="./images/s-dial_pointer.png" />
      </view>
    </block>
    <block wx:if="{{mode == 2}}">
      <image src="./images/s-dial_bg.png" style="-webkit-transform:rotate({{deg}}deg) translateZ(0);transform:rotate({{ deg}}deg) translateZ(0)" />
      <view class="wheel_pointer" bindtap="start">
        <image src="./images/s-dial_pointer.png" />
      </view>
    </block>
  </view>
</view>